# Agent workflow

Read `.agentflow/AGENT-CONTRACT.md` before project work. Reconcile and inspect
the durable workflow state with `.agentflow/agentflow.py`. Register work before
starting it, keep independent work parallel, execute vendor CLIs through the
supervisor, and accept only trusted gate receipts and terminal manifests as
evidence.
