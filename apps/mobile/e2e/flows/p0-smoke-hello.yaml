# p0-smoke-hello — Trivial smoke flow to prove the Maestro rig works end to end.
# @infra  (rig-validation flow — infrastructure, not requirement evidence)
#
# Uses a stock Android Settings launch as the APK target since the mobile skeleton
# does not exist yet (P0-17). Replace appId with OpenChat package ID once available.
#
# Determinism: no sleeps — relies on assertVisible with timeout; E2E=1 frozen clock
# assumed by the test harness.

appId: com.android.settings

---
- launchApp
