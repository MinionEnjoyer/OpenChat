# Debug Log DL-<n>

Work item: <ID> · Opened: <date> · Status: open|resolved|escalated(E-<n>)

## Observation
What was observed, verbatim (error text, test name, devctl JSON, logsnap path). No
interpretation in this section.

## Hypothesis log (append-only — never edit prior entries)

### H1
- Hypothesis (falsifiable, single cause):
- Prediction (what exactly will we observe if true):
- Experiment (ONE change/probe; exact command or diff):
- Result (verbatim output / artifact path):
- Verdict: confirmed | falsified — and why.

### H2 …

## Resolution
Root cause: <one sentence>
Fix: <commit sha>
Regression test: <file#name> (`// regression: DL-<n>`)
What would have caught this earlier (feeds BACKLOG/DRIFT-LOG if actionable):
