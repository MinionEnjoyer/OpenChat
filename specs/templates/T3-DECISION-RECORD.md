# DR-<nnn> — <short title>

Date: · Work item: · Status: proposed|accepted|superseded-by-DR-<n>

## Trigger
Which `00 §0.2.7` trigger applies (named default broken at pin / Phase-0 falsification with
E-ref / security). "Preference" is not a valid entry — if that's the honest answer, close
this DR unaccepted and follow the spec.

## Decision
One sentence: what changes from the spec default.

## Options considered
| Option | Cost | Risk | Evidence (bench/E-ref/link) |
|--------|------|------|------------------------------|

## Consequences
What downstream spec sections/items this touches; contract/CHANGELOG entries required;
tests that pin the new behavior.

## Bake-off table (if 05 §7 applied)
Metric | Variant A | Variant B → winner + why.
